# Environment Variables for HomelabFlix

# Database Configuration
POSTGRES_PASSWORD=your-secure-postgres-password-change-me
DATABASE_URL=postgresql://homelabflix:your-secure-postgres-password-change-me@postgres:5432/homelabflix

# Security Keys (CHANGE THESE IN PRODUCTION!)
# Generate a secure JWT secret: openssl rand -base64 32
JWT_SECRET=your-jwt-secret-key-min-32-characters-change-in-production

# Generate a secure encryption key: openssl rand -base64 32
# This is used to encrypt integration secrets in the database
ENCRYPTION_KEY=your-encryption-key-minimum-32-characters-required-change-in-production

# Server Configuration
PORT=3001
HOST=0.0.0.0
NODE_ENV=production
LOG_LEVEL=info

# CORS Configuration (optional)
# CORS_ORIGIN=http://localhost:3000

# Frontend Configuration
NEXT_PUBLIC_API_URL=http://localhost:3001

# Database Connection (for local development)
# DATABASE_URL=postgresql://homelabflix:changeme@localhost:5432/homelabflix
